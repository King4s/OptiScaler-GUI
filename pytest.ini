[pytest]
# Ignore test_env to prevent duplicate test module name collisions with tests/
norecursedirs = test_env
# Only consider our tests folder to avoid mistakenly picking up top-level scripts
testpaths = tests
python_files = test_*.py
